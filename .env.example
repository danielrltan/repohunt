# Copy to .env (local dev) or set in your MCP host config.
# Read-only, public-repo scope is all that is needed. The token never leaves your machine.
GITHUB_TOKEN=ghp_your_readonly_public_scope_token_here
